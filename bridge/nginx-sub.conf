server {
    listen 80;

    location /sub {
        alias /usr/share/nginx/html/;
        default_type text/plain;
        add_header Content-Disposition 'inline';
    }

    location / {
        return 404;
    }
}

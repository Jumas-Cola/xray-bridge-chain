services:
  xray-bridge:
    image: teddysun/xray
    container_name: xray-bridge
    restart: always
    ports:
      - "13335:13335/tcp"
    volumes:
      - ./config.json:/etc/xray/config.json:ro
    networks:
      - xray_net

networks:
  xray_net:
    driver: bridge
